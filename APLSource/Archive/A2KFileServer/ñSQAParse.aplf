r←ctl ⍙SQAParse data;n;names;defaults;m;i⍝ Parse argument to an SQA APL model function⍝ data is argument given by user/application n←1⊃ctl ⋄ names←2⊃ctl ⋄ defaults←3⊃ctl⍝ n        is number of positional⍝ names    is names of optional parameters⍝ defaults is r←(n↑data),defaults ⋄ data←n↓data →(∨/m←∼(⊂,2)≡¨⍴¨data)⍴e0 →(0=⍴data)⍴l0 →(∨/m←(⍴names)<i←names⍳1⊃¨data)⍴e1 r[n+i]←2⊃¨datal0:r←0 r ⋄ →0e0:r←91001 'domain error' ⋄ →0e1:r←91002('unknown parameter: ',,⍕m/names) ⋄ →0